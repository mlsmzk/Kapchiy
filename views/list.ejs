<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <title>People App</title>
    <!-- load local stylesheet (css) -->
    <link rel="stylesheet" href="/styles.css" />
    <!-- loading jquery -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
</head>
<body>
    <h1>Your query has multiple <%= kind %> results for <%= id %>! Here's what we found:</h1>

    <ol>
        <% list.forEach( (elt) => {  %>
            <% if (kind === "person") {%>
                <li><a href="nm/<%= elt.nm %>"><%= elt.name %></a></li>
            <% } else {%>
                <li><a href="tt/<%= elt.tt %>"><%= elt.title %></a></li>
            <% } %>
        <% }) %>
    </ol>
    <!-- load local js -->
    <script src="/main.js"></script>
</body>
</html>
